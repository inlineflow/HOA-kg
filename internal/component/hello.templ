package component

import "fmt"

func InterpolateGreetings(name string) string {
return fmt.Sprintf("Hello, %s", name)
}

templ Hello(name string) {
<div class="bg-yellow-">{ InterpolateGreetings(name) }</div>
}